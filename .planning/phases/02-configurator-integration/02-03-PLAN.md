---
phase: 02-configurator-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "Source Code/configurator/configurator.py"
autonomous: true
requirements:
  - CFG-10
  - CFG-11
gap_closure: true

must_haves:
  truths:
    - "DEVICE_SCREEN_MAP has a 'pico_retro' key mapped to RetroApp and no 'retro_gamepad' key"
    - "DEVICE_SCREEN_CLASSES has a 'pico_retro' key mapped to RetroApp and no 'retro_gamepad' key"
    - "When firmware reports DEVTYPE=pico_retro, the configurator routes to RetroApp without KeyError"
    - "Key 'pico_retro' is used consistently with XINPUT_SUBTYPE_TO_DEVTYPE[0x01]='pico_retro'"
  artifacts:
    - path: "Source Code/configurator/configurator.py"
      provides: "Corrected device routing keys in DEVICE_SCREEN_MAP and DEVICE_SCREEN_CLASSES"
      contains: "\"pico_retro\": RetroApp"
  key_links:
    - from: "XINPUT_SUBTYPE_TO_DEVTYPE"
      to: "DEVICE_SCREEN_CLASSES"
      via: "device_type string 'pico_retro'"
      pattern: "pico_retro.*RetroApp"
    - from: "DEVICE_SCREEN_MAP"
      to: "RetroApp"
      via: "key lookup on GET_CONFIG DEVTYPE response"
      pattern: "\"pico_retro\".*RetroApp"
---

<objective>
Fix device type routing mismatch: rename "retro_gamepad" keys to "pico_retro" in DEVICE_SCREEN_MAP and DEVICE_SCREEN_CLASSES

Purpose: The firmware's GET_CONFIG response sends DEVTYPE=pico_retro (set in retro_config.h as RETRO_DEVTYPE_STR). The configurator currently looks up that string in DEVICE_SCREEN_MAP and DEVICE_SCREEN_CLASSES, but those dicts were initially populated with the placeholder key "retro_gamepad". The lookup fails silently — RetroApp never opens. This is a two-line fix with outsized impact.

Output: Two dictionary key renames in configurator.py. No new logic, no new methods, no new files.
</objective>

<execution_context>
@C:/Users/James/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current (broken) state — keys do not match what firmware sends -->

From configurator.py DEVICE_SCREEN_MAP (look up current line number before editing):
```python
DEVICE_SCREEN_MAP = {
    ...
    "retro_gamepad": RetroApp,   # BUG: firmware sends "pico_retro", not "retro_gamepad"
    ...
}
```

From configurator.py DEVICE_SCREEN_CLASSES (look up current line number before editing):
```python
DEVICE_SCREEN_CLASSES = {
    ...
    "retro_gamepad": RetroApp,   # BUG: same mismatch
    ...
}
```

From retro_config.h (Phase 1 — locked, do not change):
```c
#define RETRO_DEVTYPE_STR "pico_retro"
```

From configurator.py XINPUT_SUBTYPE_TO_DEVTYPE (already correct — do not touch):
```python
XINPUT_SUBTYPE_TO_DEVTYPE = {
    ...
    0x01: "pico_retro",   # correct — maps XInput subtype 1 to the right string
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "retro_gamepad" to "pico_retro" in both routing dicts</name>
  <files>Source Code/configurator/configurator.py</files>
  <read_first>
    - Search for "retro_gamepad" in configurator.py to find exact line numbers before editing:
      grep -n "retro_gamepad" "Source Code/configurator/configurator.py"
    - Also confirm XINPUT_SUBTYPE_TO_DEVTYPE still uses "pico_retro" (should be untouched, verify):
      grep -n "pico_retro" "Source Code/configurator/configurator.py"
  </read_first>
  <action>
Make exactly two key renames in configurator.py. Do NOT change any other line.

**Step 1: Find both occurrences.**

Run before editing:
```
grep -n "retro_gamepad" "Source Code/configurator/configurator.py"
```

Expected output: two lines — one in DEVICE_SCREEN_MAP and one in DEVICE_SCREEN_CLASSES. Note the line numbers.

**Step 2: In DEVICE_SCREEN_MAP, change the key.**

Find the line that looks like:
```python
    "retro_gamepad": RetroApp,
```
inside DEVICE_SCREEN_MAP. Change it to:
```python
    "pico_retro": RetroApp,
```

**Step 3: In DEVICE_SCREEN_CLASSES, change the key.**

Find the line that looks like:
```python
    "retro_gamepad": RetroApp,
```
inside DEVICE_SCREEN_CLASSES. Change it to:
```python
    "pico_retro": RetroApp,
```

**What NOT to change:**
- XINPUT_SUBTYPE_TO_DEVTYPE — already correct, leave it alone
- DEVICE_TYPE_UF2_HINTS — already uses "pico_retro", leave it alone
- Any other dict or variable
- The RetroApp class itself
- Any other line in the file

These are the only two edits. The file is large — use the Edit tool with exact before/after strings to avoid touching anything else.
  </action>
  <verify>
    <automated>cd "C:/Users/James/Desktop/OCC" &amp;&amp; grep -n "retro_gamepad\|pico_retro.*RetroApp\|RetroApp.*pico_retro" "Source Code/configurator/configurator.py"</automated>
  </verify>
  <done>
    - Zero occurrences of "retro_gamepad" remain in configurator.py
    - Two occurrences of "pico_retro": RetroApp exist (one in DEVICE_SCREEN_MAP, one in DEVICE_SCREEN_CLASSES)
    - py -m py_compile "Source Code/configurator/configurator.py" exits 0
  </done>
</task>

</tasks>

<verification>
Run all three checks in sequence:

1. No stale key remains:
   grep -c "retro_gamepad" "Source Code/configurator/configurator.py"
   Expected: 0

2. Both routing dicts have the correct key:
   grep -n "pico_retro.*RetroApp" "Source Code/configurator/configurator.py"
   Expected: 2 lines (one DEVICE_SCREEN_MAP entry, one DEVICE_SCREEN_CLASSES entry)

3. Syntax is valid:
   py -m py_compile "Source Code/configurator/configurator.py"
   Expected: exits 0, no output
</verification>

<success_criteria>
The string "retro_gamepad" does not appear anywhere in configurator.py. Both DEVICE_SCREEN_MAP and DEVICE_SCREEN_CLASSES contain "pico_retro": RetroApp. The file compiles without errors. When a pico-retro device sends DEVTYPE=pico_retro, the configurator correctly routes to RetroApp.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configurator-integration/02-03-SUMMARY.md`
</output>
